// Site-wide configuration — single source of truth for base URL, site name, etc.

export const BASE_URL =
  process.env.NEXT_PUBLIC_BASE_URL || 'https://bodysignals.org';

export const SITE_NAME = 'Body Signals';
